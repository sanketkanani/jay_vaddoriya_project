import React from "react";
import Header from "../../components/Common/Header";

function NotFound() {
  return (
    <div
      style={{
        width: "100%",
        alignItems: "center",
        height: "100vh",
        display: "flex",
        justifyContent: "center",
      }}
    >
      <h1 style={{ fontSize: "24px" }}>Question Not Found</h1>
    </div>
  );
}

export default NotFound;
